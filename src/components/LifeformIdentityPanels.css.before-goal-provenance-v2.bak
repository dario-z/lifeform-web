.lifeform-identity-panel {
  position: fixed;
  z-index: 45;
  top: 0;
  right: 0;
  display: flex;
  width: min(100%, 420px);
  height: 100dvh;
  padding: 24px;
  box-sizing: border-box;
  flex-direction: column;
  gap: 16px;
  overflow-y: auto;
  color: #2d2b29;
  background: rgba(248, 244, 238, 0.98);
  border-left: 1px solid rgba(45, 43, 41, 0.14);
  box-shadow: -14px 0 36px rgba(45, 43, 41, 0.16);
  transform: translateX(104%);
  transition: transform 180ms ease;
  pointer-events: none;
}

.lifeform-identity-panel-open {
  transform: translateX(0);
  pointer-events: auto;
}

.lifeform-identity-header,
.lifeform-identity-meta,
.lifeform-identity-actions {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
}

.lifeform-identity-actions {
  align-items: center;
  justify-content: flex-start;
  flex-wrap: wrap;
}

.lifeform-identity-header h2,
.lifeform-identity-header p,
.lifeform-identity-intro,
.lifeform-identity-item p {
  margin: 0;
}

.lifeform-identity-intro {
  color: rgba(45, 43, 41, 0.7);
  line-height: 1.5;
}

.lifeform-identity-list {
  display: grid;
  gap: 12px;
  padding-bottom: 18px;
}

.lifeform-identity-item {
  display: grid;
  gap: 12px;
  padding: 14px;
  border: 1px solid rgba(45, 43, 41, 0.12);
  border-radius: 15px;
  background: rgba(255, 255, 255, 0.45);
}

.lifeform-identity-item p {
  line-height: 1.45;
}

.lifeform-identity-meta {
  color: rgba(45, 43, 41, 0.62);
  font-size: 0.8rem;
  text-transform: capitalize;
}

@media (max-width: 700px) {
  .lifeform-identity-panel {
    width: 100%;
    padding: 20px 16px;
  }
}
